{
  "name": "homed",
  "version": "1.0.0",
  "description": "",  
  "scripts": {    
    "build-schema": "npx typescript-json-schema ./tsconfig.json HomedRoot -o ./schema/homed-automation-schema.json --required --refs=false --noExtraProps --include=./interface/Homed.ts"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript-json-schema": "^0.61.0"
  }
}